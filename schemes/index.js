const { registerScheme, loginScheme } = require("./userScheme");
const { addScheme, updateFavoriteScheme } = require("./contactsScheme");

module.exports = {
    registerScheme,
    loginScheme,
    addScheme,
    updateFavoriteScheme
};